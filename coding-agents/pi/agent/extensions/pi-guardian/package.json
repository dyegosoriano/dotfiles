{
  "name": "pi-guardian-extension",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "^6.0.3"
  }
}
